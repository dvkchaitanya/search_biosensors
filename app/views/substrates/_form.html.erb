<%= form_for(@substrate) do |f| %>
  <% if @substrate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@substrate.errors.count, "error") %> prohibited this substrate from being saved:</h2>

      <ul>
      <% @substrate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
